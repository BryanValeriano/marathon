#include <bits/stdc++.h>
using namespace std;

#define pb	push_back
#define eb	emplace_back
#define mk	make_pair
#define fi	first
#define se	second
#define endl	'\n'

typedef long long ll;
typedef pair<int,int> ii;
typedef vector<ii> vii;
const int INF = 0x3f3f3f3f;
const double PI = acos(-1.0);

int mov[3][2] = {{1, 0}, {-1, 0}, {0, 1}};

bool solve(int l1, int c1, , int l2, int c2, int need) {
    if(mat[l1][c1] == 'A') return mat[l2][c2] == 'B';
    if(need) {


    } else {

    }
}

int main() {
    ios_base::sync_with_stdio(false);
    return 0;
}

